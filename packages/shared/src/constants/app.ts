export const APP_NAME = "ISAC ERP";

/**
 * Port par défaut du backend local (packages/api) sur le réseau du campus.
 * Configurable par installation via le futur module Paramètres (Module 2) —
 * cette valeur n'est qu'un défaut de développement.
 */
export const DEFAULT_LOCAL_API_PORT = 4310;

/** Port par défaut du portail web local (apps/web-portail, `next dev`) — même principe que ci-dessus. */
export const DEFAULT_LOCAL_PORTAL_PORT = 3000;
