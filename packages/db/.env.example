# Copier ce fichier en .env (jamais commité) et adapter à l'installation locale du campus.
# Valeurs par défaut alignées sur infra/docker/docker-compose.yml pour le développement.
DATABASE_URL="postgresql://isac_erp:isac_erp_dev@localhost:5432/isac_erp?schema=public"
